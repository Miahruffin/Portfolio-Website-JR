import styles from "./about.module.css"; // Import the CSS module for about.tsx

export default function AboutPage() {
    return (
        <div className={styles.container}> // Apply styles from the CSS module
        </div>
    );
}